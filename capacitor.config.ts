import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.parmanaus.app',
  appName: 'Distruition Park',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
